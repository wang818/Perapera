# 翻译结果保存功能说明

## 功能概述

翻译结果现在会自动保存为 `.txt` 格式文件到应用的 Documents 目录下。

## 工作流程

1. **视频导入** → 用户导入视频文件
2. **音频转换** → 自动将视频转换为 Opus 格式音频
3. **语音识别** → 上传音频到腾讯云 ASR 进行语音识别
4. **识别结果保存** → 识别结果保存为 JSON 文件（`{videoId}.json`）
5. **自动翻译** → 识别完成后自动触发翻译（使用混元大模型将中文翻译为日文）
6. **翻译结果保存** → 翻译结果保存为 TXT 文件（`{videoId}_translation.txt`）

## 文件保存位置

所有文件都保存在应用的 Documents 目录：

```
Documents/
├── {videoId}.json              # 识别结果（JSON 格式）
└── {videoId}_translation.txt   # 翻译结果（TXT 格式）
```

## 文件命名规则

- **识别结果文件**: `{videoId}.json`
  - 例如: `A1B2C3D4-1234567890.json`
  
- **翻译结果文件**: `{videoId}_translation.txt`
  - 例如: `A1B2C3D4-1234567890_translation.txt`

## 文件内容格式

### 识别结果 JSON 文件
```json
{
  "videoId": "A1B2C3D4-1234567890",
  "taskId": 12345,
  "recognizedText": "识别出的文本内容...",
  "timestamp": 1234567890,
  "createdAt": "2025-02-24T10:30:00Z"
}
```

### 翻译结果 TXT 文件
```
==================== 翻译结果 ====================
视频ID: A1B2C3D4-1234567890
翻译时间: 2025-02-24T10:30:00Z

【原文】
识别出的中文文本内容...

【译文（日文）】
翻訳された日本語のテキスト...
==================================================
```

## 翻译功能说明

### 翻译方法
系统使用两种翻译方法：

1. **简单文本翻译** (`translateText`)
   - 用于翻译语音识别结果
   - 将整段中文文本翻译为日文
   - 保持原文的语气和含义

2. **结构化JSON翻译** (`translateWordsToJapanese`)
   - 用于翻译包含 ResultDetail 结构的复杂JSON
   - 逐个翻译单词数组
   - 保持原有JSON结构

### 翻译流程
```
识别文本 → 解析JSON → 提取文本 → 调用混元API → 获取译文 → 保存为TXT
```

## 控制台输出

### 翻译开始
```
🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟
🚀 开始翻译识别结果
🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟🌟
```

### 翻译结果
```
================================================================================
📄 翻译结果
================================================================================
原文: 这是识别出的中文文本
译文: これは認識された中国語のテキストです
================================================================================
```

### 文件保存
```
============================================================
💾 翻译结果已保存为 TXT 文件
============================================================
📝 文件名: A1B2C3D4-1234567890_translation.txt
📂 文件路径: /path/to/Documents/A1B2C3D4-1234567890_translation.txt
📄 原文长度: 123 字符
📄 译文长度: 456 字符
============================================================
```

## 视频状态追踪

VideoStorageManager 会追踪每个视频的处理状态：

- `hasAudio`: 是否已转换音频 ✅
- `hasRecognition`: 是否已完成语音识别 ✅
- `hasTranslation`: 是否已完成翻译 ✅

这些状态会在视频列表中显示为彩色标签。

## 代码实现位置

### 主要文件

1. **HomeView.swift**
   - `saveRecognitionResultToJSON()`: 保存识别结果为JSON
   - `translateRecognitionResult()`: 解析识别结果并触发翻译
   - `saveTranslationResultToTxt()`: 保存翻译结果为TXT（包含原文和译文）

2. **HomeViewModel.swift**
   - `translate123Json()`: 翻译123.json文件的方法（用于测试）
   - `saveTranslationResultToTxt()`: 保存翻译结果的辅助方法

3. **HunyuanManager.swift**
   - `translateText()`: 翻译简单文本（新增）
   - `translateWordsToJapanese()`: 翻译结构化JSON中的单词数组
   - `generateSignature()`: 生成腾讯云API签名

4. **VideoStorageManager.swift**
   - `updateVideoRecognitionURL()`: 更新识别结果路径
   - `updateVideoTranslationURL()`: 更新翻译结果路径

## 使用示例

用户只需要导入视频，系统会自动完成以下步骤：

1. ✅ 转换音频（视频 → Opus格式）
2. ✅ 上传到COS
3. ✅ 语音识别（ASR）
4. ✅ 保存识别结果（JSON）
5. ✅ 自动翻译（中文 → 日文）
6. ✅ 保存翻译结果（TXT）

所有过程都是自动化的，用户无需手动干预。

## 技术细节

### API调用
- **混元模型**: hunyuan-lite
- **Temperature**: 0.3（较低温度确保翻译稳定）
- **TopP**: 1.0
- **编码**: UTF-8

### 错误处理
- JSON解析失败 → 输出错误信息到控制台
- API调用失败 → 显示错误提示
- 文件保存失败 → 记录错误日志

### 性能优化
- 异步处理，不阻塞UI
- 使用DispatchQueue.main.async更新UI
- 自动重试机制（识别轮询）

## 注意事项

- 翻译结果以 UTF-8 编码保存，确保中文和日文字符正确显示
- 文件名使用 videoId 确保唯一性
- 翻译失败时会在控制台输出错误信息，但不会影响其他功能
- 删除视频时，相关的音频文件会被自动删除，但识别和翻译结果文件需要手动管理
- TXT文件包含原文和译文，方便对照查看

## 未来改进

- [ ] 支持更多目标语言（英文、韩文等）
- [ ] 添加翻译质量评估
- [ ] 支持批量翻译
- [ ] 添加翻译历史记录
- [ ] 支持自定义翻译模板
